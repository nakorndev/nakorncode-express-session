extends _layout

block content
  h1.title เข้าสู่ระบบ
  hr
  form(action="/login", method="post")
    label.label ชื่อผู้ใช้ หรือ ที่อยู่อีเมล
    .field
      .control
        input.input(type="text" name="usernameOrEmail" required)
    .field
      .control
        label.label รหัสผ่าน
        input.input(type="password" name="password" required)
    hr
    .field
      button.button.is-link เข้าสู่ระบบ
